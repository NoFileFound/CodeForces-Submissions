#include <bits/stdc++.h>
#define endl '\n'
using namespace std;

int main()
{
    int T, a, b, c, d;
    cin >> T;
    while(T--)
    {
        cin >> a >> b >> c >> d;
        cout << b << ' ' << c << ' ' << c << endl;
    }

    return 0;
}